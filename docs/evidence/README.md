# Redesign Evidence

`before/` contains the baseline captures from the previous interface. `after/` contains the role-first redesign captures.

After screenshots cover:

- `sales-home.png`
- `inventory-quality-home.png`
- `packing-dispatch-home.png`
- `supervisor-home.png`
- `manager-home.png`

The screenshots are generated by `qa/tests/redesign-evidence.spec.ts` against the local Supabase-backed app. Dynamic action tests remain gated by the n8n execution quota documented in the release QA report.
